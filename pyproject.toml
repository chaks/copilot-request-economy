[project]
name = "copilot-request-economy-harness"
version = "2.0.0"
description = "Copilot CLI Request Economy Harness — hooks, routing, pruning, and budget tracking"
requires-python = ">=3.10"
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["copilot/orchestrator/lib"]
